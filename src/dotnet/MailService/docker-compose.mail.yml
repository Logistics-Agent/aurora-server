# =============================================================================
# DEPRECATED: This file has moved to deploy/local/docker-compose.yml
# =============================================================================
# MailService and Stalwart are now fully integrated into the platform-wide
# local development Docker Compose stack with profiles:
#
#   docker compose -f deploy/local/docker-compose.yml --profile mail up -d
#
# See deploy/local/README.md and docs/deployment/MAIL-PLATFORM.md for details.
# =============================================================================
